<?xml version="1.0"?>
<!--
    Module declaration. The setup_version is the schema version tracked in
    the `setup_module` table; Magento runs upgrade scripts when it changes.
-->
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework:Module/etc/module.xsd">
    <module name="Bixbox_OrderSplit" setup_version="1.0.0">
        <sequence>
            <!-- OrderSplit depends on catalog (attributes/products),
                 quote/checkout (order placement) and sales (orders). -->
            <module name="Magento_Catalog"/>
            <module name="Magento_Quote"/>
            <module name="Magento_Checkout"/>
            <module name="Magento_Sales"/>
        </sequence>
    </module>
</config>
