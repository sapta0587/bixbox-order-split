{
    "name": "bixbox/module-order-split",
    "description": "Bixbox_OrderSplit - Magento 2 module: display vendor & warehouse area on product pages and split orders by (category, vendor, warehouse area).",
    "type": "magento2-module",
    "license": "MIT",
    "version": "1.0.0",
    "require": {
        "php": "~8.1.0||~8.2.0||~8.3.0||~8.4.0||~8.5.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5"
    },
    "suggest": {
        "magento/module-catalog": "Provides the product EAV the module reads from. Required when installed into a Magento 2 store; not needed to run the standalone unit tests."
    },
    "autoload": {
        "psr-4": {
            "Bixbox\\OrderSplit\\": "app/code/Bixbox/OrderSplit/",
            "Bixbox\\PaymentWebhook\\": "app/code/Bixbox/PaymentWebhook/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Bixbox\\OrderSplit\\Test\\": "app/code/Bixbox/OrderSplit/Test/",
            "Bixbox\\PaymentWebhook\\Test\\": "app/code/Bixbox/PaymentWebhook/Test/"
        }
    },
    "_comment_order": "registration.php is intentionally NOT autoloaded here; it is only relevant when the module is installed into a real Magento (see the module composer.json files). This keeps the standalone unit-test bootstrap Magento-free."
}
